정점셰이더
wvp변환

기하셰이더(지오메트리셰이더)
공간변환

테셀레이터
정점증가

레스터라이저
정점 -> 픽셀,
뒷면 제거(컬링), (화면에서 잘림(클립)),
와이어프레임/솔리드,

보간기
선형보간

테셀레이션
기존 파이프라인과 크게 다름

create 부분은 자주 사용된다면 변수로 뽑아서
새로 생성하지 않고 교체하는 형식으로 최적화 진행

스카이스피어
1. 원을 크게 만들고 그 안쪽에서 하는 방법
2. 카메라 앞에 원을 달고 다니며 하늘을 그리고 depth값을 제일 뒤로 주어
	모델을 그 앞에 그리는 방법
	
JSON / XML : json이 가벼워서 자주 쓰임
자바스크립트의 객체문법

------------------------------------------------------------

언리얼 컨텐츠 = Game 경로

비주얼 이펙트 - 리플렉션 캡쳐
반사정보 수집 크게 설정되어있음 - 범위(기즈모 or 볼륨)
정보를 만든다(bake or build) - 그림파일 형태로 저장

- 스카이라이트
간접광 효과 디스턴스 스레쉬 홀드 에 설정된 거리에서 빛 가져옴

- 스카이스피어
하늘 그려줌

- 아트모스페릭 포그
대기 산란

- 디렉셔널 라이트 / 포인트 라이트 / 스포트 라이트
방향광(태양) / 점광 / 하이라이트(원뿔)

GI는 사전에 빛을 구운 곳에서 실시간 Light를 더함
디퍼드 빛 계산을 나중에 더함 수집하여 간접광을 계산
랜더타겟이 화면출력이 아닌 텍스쳐로 나와야 가능함

모빌리티(랜더링엔진/물리엔진)
스태틱(고정) 스테이셔너리(고정이지만 추가효과가능, 4개 제한) 무버블(움직임)-간접광 계산에서 제외

액터 병합
물체 여러개를 묶어서 하나의 물체로 합칠 수 있음(피봇 변환도 가능)
하나를 병합하여 피봇 등 설정 변환 가능

스케일 자물쇠
비율을 x,y,z에 전부 적용

레벨
액너가 올라가있는 상태
맵
리소스들 총합


